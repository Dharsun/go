def search_in_folder(folder_path, keyword_patterns):
    results = {keyword: [] for keyword in keyword_patterns.keys()}

    for root, _, files in os.walk(folder_path):
        for file_name in files:
            file_path = os.path.join(root, file_name)
            with open(file_path, 'rb') as file:  # Open the file in binary mode
                content = file.read()  # Read the content without decoding
                if isinstance(content, bytes):
                    # Handle binary content, skip processing
                    continue
                
                for keyword, pattern in keyword_patterns.items():
                    matches = re.findall(pattern, content, re.IGNORECASE)
                    if matches:
                        results[keyword].extend([(match, file_path) for match in matches])

    return results
